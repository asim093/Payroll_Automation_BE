# Azure AD tenant ID for the app registration used to call Microsoft Graph
TENANT_ID=

# Azure AD application (client) ID
CLIENT_ID=

# Azure AD application client secret
CLIENT_SECRET=

# MongoDB connection string (Atlas or local), including the database name
MONGODB_URI=

# Mailbox address to poll via Microsoft Graph. Leave empty until a real
# mailbox is available — see backend/SETUP_MAILBOX.md
TEST_MAILBOX_EMAIL=

# Port the Express server listens on
PORT=5000

# Dropbox API credentials
DROPBOX_APP_KEY=
DROPBOX_APP_SECRET=
# DEPRECATED - short-lived static token, no longer read by the code.
# Kept only for reference; DROPBOX_REFRESH_TOKEN below replaces it.
DROPBOX_ACCESS_TOKEN=
# Long-lived refresh token — obtained by running `node getDropboxRefreshToken.js`
# once and pasting its console output here. Never share this value.
DROPBOX_REFRESH_TOKEN=

# Delegated (Authorization Code Flow) auth — for personal Microsoft accounts
# (e.g. outlook.com), separate from the client-credentials flow above.
# Same app as CLIENT_ID, kept as its own variable since this is a parallel flow.
DELEGATED_CLIENT_ID=
# Local redirect URI registered on the app for the Authorization Code Flow
REDIRECT_URI=http://localhost:3000/redirect
# Long-lived refresh token — obtained by running `node getRefreshToken.js`
# once and pasting its console output here. Never share this value.
DELEGATED_REFRESH_TOKEN=
# Mailbox the delegated flow polls via "me" — used only for logging, since
# "me" already resolves to whichever account DELEGATED_REFRESH_TOKEN belongs to.
DELEGATED_MAILBOX_EMAIL=

# PHASE-UI-13 — web-hosted alternative to running getRefreshToken.js
# locally, for when the person who needs to log in (e.g. the client, on
# their own mailbox) isn't at the machine running the script. Send them
# https://<this-backend's-public-URL>/oauth/microsoft/start?token=<OAUTH_SETUP_SECRET>
# — they log in in their own browser, and the resulting refresh token is
# saved straight to MongoDB (shared by the web service and both cron jobs,
# no per-service env-var copying needed - see delegatedAuthService.js).
#
# Must be this backend's own public URL + /oauth/microsoft/callback, and
# the EXACT same string registered as a Redirect URI on the Azure app
# registration (Authentication > Web > Redirect URIs).
MICROSOFT_OAUTH_CALLBACK_URL=
# Any long random string - required as a ?token= query param on
# /oauth/microsoft/start so this isn't a fully public "re-authorize our
# mailbox integration" link. Never share this value itself, only the full
# link built from it.
OAUTH_SETUP_SECRET=

# ShareFile API credentials
SHAREFILE_CLIENT_ID=
SHAREFILE_CLIENT_SECRET=
SHAREFILE_SUBDOMAIN=
# Legacy OAuth2 password-grant fallback - only used if no OAuth login (see
# SHAREFILE_OAUTH_CALLBACK_URL below) has been completed yet. Storing the
# account's real password here is exactly what the hosted login flow below
# exists to avoid - prefer that instead once it's set up.
SHAREFILE_USERNAME=
SHAREFILE_PASSWORD=

# PHASE-UI-14 — web-hosted ShareFile login flow (mirrors
# MICROSOFT_OAUTH_CALLBACK_URL above). Send whoever owns the ShareFile
# account https://<this-backend's-public-URL>/oauth/sharefile/start?token=<OAUTH_SETUP_SECRET>
# — they log in directly on ShareFile's own page (their password is never
# seen by this app), and the resulting refresh token is saved to MongoDB.
#
# Must be this backend's own public URL + /oauth/sharefile/callback, and
# the EXACT same string must replace the app's default Redirect URI
# (secure.sharefile.com/oauth/oauthcomplete.aspx) in the ShareFile API App
# Console.
SHAREFILE_OAUTH_CALLBACK_URL=

WEB_SERVICE_INTERNAL_URL=

INTERNAL_NOTIFY_SECRET=
